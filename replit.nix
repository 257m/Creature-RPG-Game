{ pkgs }: {
	deps = [
    	pkgs.gh
     pkgs.unar
    	pkgs.indent
  		pkgs.cloc
  		pkgs.htop
  		pkgs.vim
  		pkgs.zip
  		pkgs.valgrind
  		pkgs.pkgconfig
  		pkgs.clang_12
		pkgs.ccls
		pkgs.gdb
		pkgs.gnumake
		pkgs.glfw
		pkgs.libGLU
		pkgs.stb
	];
}